<%= render 'shared/header', header: "Containers" do %>
  <%= link_to "New container", new_container_path %>
<% end %>

<div id="containers">
  <div class="table-responsive">
    <table class="table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Income</th>
          <th>Expense</th>
          <th>Balance</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
          <% @containers.each do |container| %>
            <%= render container %>
          <% end %>
      </tbody>
    </table>
  </div>
</div>

