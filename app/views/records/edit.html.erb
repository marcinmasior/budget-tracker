<%= render 'shared/header', header: 'Editing record' do %>
  <%= link_to @container, class: "btn btn-secondary" do %>
    <i class="bi bi-arrow-left"></i> Back to container
  <% end %>
<% end %>

<%= render "form", container: @container, record: @record %>